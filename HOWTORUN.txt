navigate into productivity_app root directory

!! if you do not already have node_modules folder (it is ignored by github) type 'npm i'

type 'npm run build' in console
    - this bundles the react code

type 'rpm run start' in console
    - this starts the Electron app
    
this should start the electronJS window

!! to close the window in terminal = ctrl + c